import 'package:flutter/material.dart';

class BookingDetailsBody extends StatelessWidget {
  const BookingDetailsBody({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
